SELECT
	{[Measures].[Profit]} ON COLUMNS,
	Order(
		TopCount(
			[Product.ProductCategory].[All Product.ProductCategorys].Children,
			[Measures].[Profit]
		),
		[Measures].[Profit],
		DESC
	) ON ROWS
FROM
	[bike_sales]
WHERE
	[OrderDate].[Year].&[2021]
